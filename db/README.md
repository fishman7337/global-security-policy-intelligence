# Database

This folder contains database schema assets for local and containerized
analytics infrastructure.

`schema.sql` defines the relational/PostGIS-facing tables used by the API and
artifact loaders. Keep schema changes synchronized with pipeline output,
repository methods, and API tests.
