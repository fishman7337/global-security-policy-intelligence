# Database Loaders

This package contains database loading utilities.

Use these modules to move curated artifacts into database-backed analytics
tables. Keep schema assumptions synchronized with `db/schema.sql` and avoid
embedding credentials in code.
