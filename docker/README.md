# Docker

This folder contains container build definitions for the API and frontend.

Use these Dockerfiles with the root `docker-compose.yml` for local integrated
runs. Keep image changes reproducible, avoid baking secrets into images, and
prefer documented environment variables for configuration.
