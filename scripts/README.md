# Scripts

This folder contains command-line helpers for artifact builds, notebook
rendering, local API runs, severity training, and larger local build flows.

Scripts should be idempotent where possible, document required environment
variables, and avoid writing outside the repository's expected artifact
locations.
