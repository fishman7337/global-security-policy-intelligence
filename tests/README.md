# Tests

This folder contains backend, documentation, policy, graph, RAG, W&B, and
pipeline tests.

Run the test suite from the repository root:

```powershell
pytest
```

Add or update tests whenever behavior, data contracts, API outputs, notebooks,
or safety guardrails change.
