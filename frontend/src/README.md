# Frontend Source

This folder contains the dashboard source code.

`main.tsx` defines the routed application views and `api.ts` centralizes API
calls to the FastAPI backend. Keep UI changes consistent with aggregate
research workflows and preserve visible limitations where model or policy
claims could be overread.
